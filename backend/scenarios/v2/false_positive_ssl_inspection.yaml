# Schema v2 (Phase 2 Stage 0). Migrated from scenarios/false_positive_ssl_inspection.yaml by
# scenarios/migrate_v1_to_v2.py; the attack steps are the v1 chain
# byte-for-byte plus id/host/user tags (parity_check_v2.py proves it).
# Edit this file, not the v1 copy, once the loader flag flips to yaml_v2.
schema_version: 2
label: "false_positive_ssl_inspection"
category: "False Positive"
difficulty: 3
threat_pattern: "Authorized SSL Inspection \u2014 Corporate Proxy CA Breaking Certificate Pinning"

narrative:
  ticket_title: "Repeated TLS Failures"
  storyline: "Multiple workstations are generating repeated failed TLS connections to Microsoft 365 endpoints. The connection pattern resembles command-and-control beaconing with consistent retry intervals. The network team recently expanded the corporate proxy's SSL inspection policy to cover additional domains."

entities:
  victim:
    type: "employee"
  m365_endpoint:
    type: "external_ip"
    ip: "52.96.84.228"

environment:
  org:
    name: "ACME Corp"
    domain: "acme.local"
  hosts:
    - id: "ws_victim"
      role: "workstation"
      hostname: "{victim.hostname}"
      ip: "{victim.ip}"
      os: "Windows 11 Enterprise"
      desc: "User workstation"
    - id: "proxy"
      role: "proxy"
      hostname: "{infra.proxy.hostname}"
      ip: "{infra.proxy.ip}"
      os: "PAN-OS"
      desc: "Proxy (PAN-OS VM-Series)"
    - id: "fw_perimeter"
      role: "firewall"
      hostname: "ACME-FW01"
      ip: "10.0.1.254"
      os: "PAN-OS"
      desc: "Perimeter Firewall"
  accounts:
    - id: "victim"
      username: "{victim.username}"
      domain: "ACME"
      type: "user"
      host: "ws_victim"

attack:
  - id: "s1"
    host: "ws_victim"
    user: "victim"
    offset: 0
    event_type: "SSL_INSPECT"
    source_type: "Proxy"
    log_source: "Proxy"
    severity: "medium"
    hostname: "{victim.hostname}"
    source_ip: "{victim.ip}"
    destination_ip: "{m365_endpoint.ip}"
    user_account: "ACME\\{victim.username}"
    message: "SSL inspection applied to TLS session to outlook.office365.com"
    key_value_pairs:
      src_ip: "{victim.ip}"
      dst_ip: "{m365_endpoint.ip}"
      src_user: "{victim.username}"
      dvc: "{victim.hostname}"
      url: "outlook.office365.com:443"
      app: "ssl"
      transport: "tcp"
      dest_port: "443"
      action: "inspect"
      process: "OUTLOOK.EXE"
      ssl_action: "inspect"
      original_cert_issuer: "CN=Microsoft Azure TLS Issuing CA 01, O=Microsoft Corporation, C=US"
      presented_cert_issuer: "CN=ACME-PROXY-CA, O=ACME Corp, C=US"
      ssl_policy: "Inspect-All-HTTPS"

  - id: "s2"
    host: "ws_victim"
    user: "victim"
    offset: [5, 15]
    trigger: true
    event_type: "SSL_HANDSHAKE_FAILED"
    source_type: "Proxy"
    log_source: "Proxy"
    severity: "high"
    hostname: "{victim.hostname}"
    source_ip: "{victim.ip}"
    destination_ip: "{m365_endpoint.ip}"
    user_account: "ACME\\{victim.username}"
    message: "TLS handshake failed for connection to outlook.office365.com"
    key_value_pairs:
      src_ip: "{victim.ip}"
      dst_ip: "{m365_endpoint.ip}"
      src_user: "{victim.username}"
      dvc: "{victim.hostname}"
      url: "outlook.office365.com:443"
      app: "ssl"
      transport: "tcp"
      dest_port: "443"
      action: "deny"
      process: "OUTLOOK.EXE"
      tls_alert: "certificate_unknown"
      tls_alert_code: "46"
      failure_reason: "Client sent TLS fatal alert"
      presented_cert_issuer: "CN=ACME-PROXY-CA, O=ACME Corp, C=US"
      expected_cert_issuer: "CN=Microsoft Azure TLS Issuing CA 01"
      connection_result: "TERMINATED"
      failure_count_last_hour: "47"

  - id: "s3"
    host: "ws_victim"
    user: "victim"
    offset: [2, 5]
    event_type: "ALLOW"
    source_type: "Firewall"
    log_source: "Firewall"
    severity: "high"
    hostname: "ACME-FW01"
    source_ip: "{victim.ip}"
    destination_ip: "{m365_endpoint.ip}"
    user_account: "ACME\\{victim.username}"
    message: "Outbound connection permitted to {m365_endpoint.ip} on port 443"
    key_value_pairs:
      src_ip: "{victim.ip}"
      dst_ip: "{m365_endpoint.ip}"
      src_port: "51443"
      dest_port: "443"
      transport: "tcp"
      action: "allow"
      app: "ssl"
      dvc: "ACME-FW01"
      rule: "https_outbound"

  - id: "s4"
    host: "ws_victim"
    user: "victim"
    offset: [3, 8]
    event_type: "NetworkConnect"
    source_type: "Sysmon"
    log_source: "Sysmon"
    severity: "medium"
    hostname: "{victim.hostname}"
    source_ip: "{victim.ip}"
    destination_ip: "{m365_endpoint.ip}"
    user_account: "ACME\\{victim.username}"
    message: "Outbound connection to {m365_endpoint.ip}:443 from OUTLOOK.EXE"
    key_value_pairs:
      event_id: 3
      channel: "Microsoft-Windows-Sysmon/Operational"
      computer: "{victim.hostname}"
      rule_name: ""
      process_id: "4492"
      image: "C:\\Program Files\\Microsoft Office\\root\\Office16\\OUTLOOK.EXE"
      user: "{victim.user_domain}"
      protocol: "tcp"
      initiated: true
      source_is_ipv6: false
      source_ip: "{victim.ip}"
      source_hostname: "{victim.hostname}"
      source_port_name: ""
      destination_is_ipv6: false
      destination_ip: "{m365_endpoint.ip}"
      destination_hostname: "outlook.office365.com"
      destination_port: "443"
      destination_port_name: "https"

  - id: "s5"
    host: "ws_victim"
    user: "victim"
    offset: [10, 30]
    event_type: "SSL_INSPECT"
    source_type: "Proxy"
    log_source: "Proxy"
    severity: "low"
    hostname: "{victim.hostname}"
    source_ip: "{victim.ip}"
    user_account: "ACME\\{victim.username}"
    message: "SSL inspection applied \u2014 certificate reissued for outlook.office365.com"
    key_value_pairs:
      src_ip: "{victim.ip}"
      dst_ip: "52.96.165.18"
      src_user: "{victim.username}"
      dvc: "{infra.proxy.hostname}"
      url: "https://outlook.office365.com/owa/"
      app: "ssl"
      transport: "tcp"
      dest_port: "443"
      action: "decrypted"
      http_method: "CONNECT"
      ssl_inspected: true
      ssl_policy: "corporate-tls-inspection"
      server_cert_subject: "CN=outlook.office365.com"
      server_cert_issuer: "CN=Microsoft Azure TLS Issuing CA 01, O=Microsoft Corporation, C=US"
      reissued_cert_issuer: "CN=ACME-PROXY-CA, O=ACME Corp, C=US"
      tls_version: "TLSv1.3"
      category: "business/productivity"

noise: {}

answer_key:
  classification: "False Positive"
  scope:
    hosts: ["ws_victim"]
    accounts: ["victim"]
  root_cause: null
  techniques: []
  actions: []

triage_review:
  what_is_it:
    title: "SSL Inspection Policy Expansion"
    description: "When a corporate proxy expands its SSL inspection scope to new domains, endpoints may generate repeated TLS handshake failures while certificate trust is established. The retry pattern with consistent intervals to fixed destinations closely resembles command-and-control communication."
  response_actions:
    - "Check with the network team for recent proxy policy changes"
    - "Verify the destination domains are legitimate Microsoft 365 endpoints"
    - "Confirm the TLS failures correlate with the inspection policy rollout timeline"
    - "Push updated root certificates to affected workstations"
    - "Monitor for resolution as endpoints pick up the new trust chain"
