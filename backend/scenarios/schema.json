{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Spectyr scenario file",
  "type": "object",
  "required": ["label", "category", "difficulty", "threat_pattern", "narrative", "entities", "chain", "triage_review"],
  "additionalProperties": false,
  "properties": {
    "label": { "type": "string", "pattern": "^[a-z0-9_]+$" },
    "category": {
      "enum": [
        "Malware", "Phishing", "Defense Evasion", "Lateral Movement",
        "Command & Control", "Brute Force", "Data Exfiltration",
        "Insider Threat", "False Positive"
      ]
    },
    "difficulty": { "type": "integer", "minimum": 1, "maximum": 3 },
    "threat_pattern": { "type": "string", "minLength": 1 },
    "narrative": {
      "type": "object",
      "required": ["ticket_title", "storyline"],
      "additionalProperties": false,
      "properties": {
        "ticket_title": { "type": "string", "minLength": 1 },
        "storyline": { "type": "string", "minLength": 1 }
      }
    },
    "entities": {
      "type": "object",
      "propertyNames": { "pattern": "^(?!infra$)[a-z][a-z0-9_]*$" },
      "additionalProperties": { "$ref": "#/$defs/entity" }
    },
    "chain": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/step" }
    },
    "triage_review": {
      "type": "object",
      "required": ["what_is_it", "response_actions"],
      "additionalProperties": false,
      "properties": {
        "mitre": {
          "type": "object",
          "required": ["id", "name", "tactic", "url"],
          "additionalProperties": false,
          "properties": {
            "id": { "type": "string", "pattern": "^T[0-9]{4}(\\.[0-9]{3})?$" },
            "name": { "type": "string" },
            "tactic": { "type": "string" },
            "url": { "type": "string", "format": "uri" }
          }
        },
        "what_is_it": {
          "type": "object",
          "required": ["title", "description"],
          "additionalProperties": false,
          "properties": {
            "title": { "type": "string" },
            "description": { "type": "string" }
          }
        },
        "response_actions": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" }
        }
      }
    }
  },
  "$defs": {
    "entity": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "enum": ["employee", "external_ip", "internal_host", "domain", "file", "hash"]
        },
        "value": { "type": "string" },
        "hostname": { "type": "string" },
        "ip": { "type": "string" }
      }
    },
    "step": {
      "type": "object",
      "required": ["offset", "event_type", "source_type", "severity", "message"],
      "additionalProperties": false,
      "properties": {
        "offset": {
          "oneOf": [
            { "type": "number", "minimum": 0 },
            {
              "type": "array",
              "items": { "type": "number", "minimum": 0 },
              "minItems": 2,
              "maxItems": 2
            }
          ]
        },
        "event_type": { "type": "string", "minLength": 1 },
        "source_type": { "type": "string", "minLength": 1 },
        "log_source": { "type": "string" },
        "severity": { "enum": ["low", "medium", "high", "critical"] },
        "trigger": { "type": "boolean" },
        "hostname": { "type": "string" },
        "source_ip": { "type": "string" },
        "destination_ip": { "type": "string" },
        "user_account": { "type": "string" },
        "message": { "type": "string", "minLength": 1 },
        "key_value_pairs": { "type": "object" }
      }
    }
  }
}
